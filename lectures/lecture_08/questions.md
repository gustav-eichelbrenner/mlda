# Вопросы для самопроверки

1. В чем принципиальная разница между метриками оценки для задач регрессии и классификации?
2. Дайте определение метрик MAE (Mean Absolute Error) и MSE (Mean Squared Error). В каких случаях предпочтительнее использовать MAE?
3. Что показывает коэффициент детерминации ($R^2$) и какие значения он может принимать?
4. Почему использование тестовой выборки для подбора гиперпараметров является методологической ошибкой?
5. Опишите алгоритм проведения стратифицированной кросс-валидации (Stratified K-Fold).
6. Что такое «базовая модель» (baseline model) и зачем ее строить перед обучением сложных алгоритмов?
7. Как временные ряды влияют на стратегию разделения данных (почему нельзя использовать случайное разбиение)?
8. В чем смысл использования метрики RMSE (Root Mean Squared Error) вместо MSE?
9. Как оценить статистическую значимость различий в качестве двух моделей?
10. Что такое доверительный интервал для метрики качества и как его рассчитать с помощью бутстрапа?
